import Login from "@/components/auth/Login";

export const metadata = {
  title: "Login",
  description:
    "Sign in to your Sell It account to manage your listings, messages, and marketplace activity.",
};

const LoginPage = () => {
    return <Login />;
};

export default LoginPage;